name: paths-B-pull_request-path-test

on: 
  pull_request:
    paths:
    - "paths-B/**"
    - "!paths-B/**.md"
  push: 

jobs:

  build:
    name: paths-B-ls
    runs-on: ubuntu-latest
    steps:
    - run: ls paths-B
    - run: ls
